class Model:

    def __init__(self, layers):
        """
        Contructor of Model class

        Parameters:
        layers(Layer): list of layers which compose the model
        
        """
        self.layers = layers